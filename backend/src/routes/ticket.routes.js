







export default router; router.get("/venue-layout/:eventId", getVenueSeatLayout); const router = express.Router(); import { getVenueSeatLayout } from "../controllers/venue.controller.js"; import express from "express";